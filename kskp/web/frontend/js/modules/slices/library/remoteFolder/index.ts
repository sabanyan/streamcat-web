/*
import { createSlice } from '@reduxjs/toolkit'

const initialState = {
  label: "",
  protocol: "",
  hostname: "",
  domain: "",
  path: "",
  user_id: "",
  password: ""
}

export const remoteFolderSlice = createSlice({
  name: 'remoteFolder',
  initialState: initialState,
  reducers: {
    add(state) {
      state = { ...initialState }
    },
    edit(state, action) {
      state = { ...action.payload }
    }
  }
});

export default remoteFolderSlice;
*/